# created 11:40:28 - 20/08/09
# Maintainer: Frikilinux <frikilinux at frikilinux.com.ar>

pkgname=kpassgen
pkgver=1.2
pkgrel=1
pkgdesc="Random password generator"
arch=('i686' 'x86_64')
url="http://www.kde-apps.org/content/show.php/KPassGen?content=108673"
license=('GPL2')
depends=('kdelibs')
makedepends=('cmake' 'automoc4')
source=("http://downloads.sourceforge.net/${pkgname}/${pkgname}-${pkgver}.tar.gz")

build() {
	cd ${pkgname}
	mkdir -p build
	cd build
	cmake -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=RELEASE ..
	make
}
package() {
	cd ${pkgname}/build
	make DESTDIR="${pkgdir}" install
}
sha256sums=('0cbfde61f38fc43b9b7883c823f3da7326ab00697380de2383da2967119b0152')
