 
# Contributor: said <atvordhosbn[at]gmail[dot]com>

pkgname=libcue
pkgver=1.4.0
pkgrel=1
pkgdesc="A fork of cuetools."
arch=('i686' 'x86_64')
url="http://libcue.sourceforge.net/"
license=('GPL2/CUSTOM')
depends=('glibc')
makedepends=('autoconf' 'automake')
source=(http://downloads.sourceforge.net/libcue/$pkgname-$pkgver.tar.bz2)
md5sums=('5f5045f00e6ac92d9a057fe5b0982c69')

build() {
cd $srcdir/$pkgname-$pkgver

./autogen.sh --prefix=/usr
make || return 1
make DESTDIR=$pkgdir install || return 1

# Install license
install -Dm644 COPYING $pkgdir/usr/share/licenses/$pkgname/COPYING
}
 
