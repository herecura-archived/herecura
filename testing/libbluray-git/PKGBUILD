# vim:set ft=sh:
# Maintainer: Andrew Cook <ariscop@gmail.com>

pkgname=libbluray-git
_gitname="libbluray"
pkgver=20130412.8645255
pkgrel=1
pkgdesc="Blu-Ray access library"
arch=('i686' 'x86_64')
license=('LGPL')
url="http://www.videolan.org/developers/libbluray.html"
depends=('openssl')
makedepends=('git' 'apache-ant' 'java-environment' 'freetype2')
optdepends=('libaacs' 'libbdplus' 'java-runtime')
source=("$_gitname::git://git.videolan.org/libbluray.git")
sha256sums=('SKIP')
provides=('libbluray')
conflicts=('libbluray')

pkgver() {
	cd "$srcdir/$_gitname"
	echo $(git log -1 --format="%ci" | sed 's/.*\([0-9]\{4\}\)-\([0-9]\{2\}\)-\([0-9]\{2\}\).*/\1\2\3/').$(git rev-parse --short HEAD)
}

build() {
    cd $srcdir/$_gitname
	sed -e 's/AM_CONFIG_HEADER/AC_CONFIG_HEADERS/g' -i configure.ac
    ./bootstrap
    ./configure --prefix=/ --with-jdk=$J2SDKDIR --enable-bdjava
    make
}

package() {
    cd $srcdir/$_gitname
    make DESTDIR=$pkgdir/usr install
}
