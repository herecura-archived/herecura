# Maintainer: BlackEagle < ike DOT devolder AT gmail DOT com >
# vim:syntax=sh
pkgname=closure-linter
pkgver=2.2.6
pkgrel=1
pkgdesc="A JavaScript style checker and style fixer"
arch=('any')
url="http://code.google.com/closure"
license=('APACHE')
depends=('python2' 'python-gflags')
makedepends=('python2-distribute')
source=("http://${pkgname}.googlecode.com/files/${pkgname/-/_}-${pkgver}.tar.gz")
sha256sums=('1ab0e890d675c9a60d839b8c4fcbac8279a93f251338401d138769c442c25f8f')

build() {
	cd ${pkgname/-/_}-${pkgver}
	python2 setup.py build
}

package() {
	cd ${pkgname/-/_}-${pkgver}
	python2 setup.py install --root=${pkgdir}
	if [ -d ${pkgdir}/usr/lib/$(python2-config --libs | sed 's/.*\(python.*\)/\1/g')/site-packages/closure_linter-2.2.5-py$(python2-config --libs | sed 's/.*\(python.*\)/\1/g' | sed 's/[a-Z]*//').egg-info ]; then
		cd ${pkgdir}/usr/lib/$(python2-config --libs | sed 's/.*\(python.*\)/\1/g')/site-packages/closure_linter-2.2.5-py$(python2-config --libs | sed 's/.*\(python.*\)/\1/g' | sed 's/[a-Z]*//').egg-info/
		chmod u=rw,og=r *
	fi
}
